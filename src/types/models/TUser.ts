interface User {
  id: string;
  username: string;
  firstName: string;
  lastName: string;
  profileImageUrl: string;
  backgroundImageUrl: string;
  bio: string;
  birthdate: string;
}

export default User;