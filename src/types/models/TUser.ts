interface User {
  id: number;
  userName: string;
  firstName: string;
  lastName: string;
  profileImageUrl: string;
  backgroundImageUrl: string;
  bio: string;
  birthDate: string;
}

export default User;